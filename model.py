# from keras.models import load_model
# import numpy as np
# import h5py

# model = load_model("new.h5")
# model._make_predict_function()

# def predict_model(trig):
#     pred=model.predict(trig)
#     print(pred)

# predict_model("ADANIPORTS")
